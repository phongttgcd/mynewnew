
@using COMP1640_WebDev.ViewModels
@model MagazineViewModel

@{
	Layout = "./Layout/_MMLayout";
}


<div class="px-4 mt-4 min-vh-100">
	<h1>Create Magazine</h1>

	<form asp-action="CreateMagazine" method="post" enctype="multipart/form-data">
		@Html.AntiForgeryToken()
		<div class="form-group mb-2">
			<label asp-for="@Model.FormFile">Choose files to upload:</label>
			<input asp-for="@Model.FormFile" type="file" class="form-control" />
		</div>

		<div class="form-group mb-2">
			<label asp-for="Magazine!.Title" class="control-label">Title</label>
			<input asp-for="Magazine!.Title" class="form-control" />
			<span asp-validation-for="Magazine!.Title" class="text-danger"></span>
		</div>

	@* 	Tan Phong Sua label ben duoi thanh Description *@
		<div class="form-group mb-2">
			<label asp-for="Magazine!.Description" class="control-label">Title</label>
			<input asp-for="Magazine!.Description" class="form-control" />
			<span asp-validation-for="Magazine!.Description" class="text-danger"></span>
		</div>

		<div class="form-group mb-2">
			<label>Faculty</label>
			<select asp-for="Magazine!.FacultyId"
					asp-items="@(new SelectList(Model.Falulties, "Id", "FacultyName"))"
					class="form-control">
			</select>
			<span asp-validation-for="Magazine!.FacultyId" class="text-danger"></span>

		</div>

		<div class="form-group mb-2">
			<label>Semester</label>
			<select asp-for="Magazine!.AcademicYearId"
					asp-items="@(new SelectList(Model.AcademicYears, "Id", "StartDate"))"
					class="form-control">
			</select>
			<span asp-validation-for="Magazine!.AcademicYearId" class="text-danger"></span>
		</div>



		<div class="form-group">
			<input type="submit" value="Create" class="btn btn-primary" />
		</div>
	</form>
</div>



